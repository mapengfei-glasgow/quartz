请根据以下文件结构和示例模板，对上面文章中的生词生成单词解释。

**文件结构要求**：
- 每个单词单独一个章节，以单词本身作为标题（使用 `#` 一级标题）
- 包含 **音标**（使用国际音标，放在 `**音标**:` 后）
- 包含 **词性**（如 n./v./adj./phr.v. 等，放在 `**词性**:` 后）
- 包含 **释义**（中文解释，可包含扩展说明，放在 `**释义**:` 后）
- 包含 **例句（原文）**（从文章中摘取原句，用 `**例句** (原文):` 引出）

**示例模板**：

# walking

**音标**: /ˈwɔːkɪŋ/

**词性**: phr.v.

**释义**: **walk sb through sth** 引导某人逐步了解；向某人详细讲解某事的每个步骤，常用于演讲、报告等场合

**例句** (原文): Callan would be walking investors through the report in a conference call...

---

需要解释的生词列表已经标注好，请严格按照上述模板格式，为每个生词生成单词解释。每个单词为一个文件，放入 content/learning-english/dictionary 路径下，以使obsidian的wiki能够使用。


